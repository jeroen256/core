{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build"
                // next arg commented out because sln file exists, all projects referenced there are build automatically without having to specify each one separately
                // ,"${workspaceFolder}/server/server.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}